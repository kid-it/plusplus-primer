#include <iostream>
#include <string> 

//NOTE: this will not compile

/* function named compare that returns a bool,
and has two parameters that are references to 
a class called matrix */ 
bool return(const matrix &m1, const matrix &m2);

std::vector<int>::iterator change_val(int, std::vector<int>::iterator);